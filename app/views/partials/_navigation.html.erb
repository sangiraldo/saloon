<nav class="black" role="navigation">
  <div class="nav-wrapper">
    <%= link_to 'Saloon', root_path, class: "brand-logo" %>
    <ul class="right hide-on-med-and-down" data-turbolinks="false">
      <% if user_signed_in? %>
        <a class="dropdown-button btn grey darken-1 profile" data-beloworigin="true"
        data-activates="dropdown1"><%= current_user.full_name %></a>

        <ul id="dropdown1" class="dropdown-content">
          <li><%= link_to  t('profile'), edit_user_registration_url %></a></li>
          <% if current_user.is_admin? %>
            <li> <%= link_to t('admin_panel'), admin_root_url %></li>
          <% end %>
          <li class="divider"></li>
          <li> <%= link_to t('sign_out'), destroy_user_session_url, method: :delete %></li>
        </ul>
      <% else %>
        <li><%= link_to  t('sign_in'), new_user_session_url, class: "btn grey darken-1" %></a></li>
        <li><%= link_to  t('sign_up'), new_user_registration_url, class: "btn grey darken-1" %></a></li>
      <% end %>

    </ul>
    <ul id="nav-mobile" class="side-nav">
      <% if user_signed_in? %>
        <li><%= link_to(t('profile'), edit_user_registration_url) %></li>
        <% if current_user.is_admin? %>
          <li> <%= link_to t('admin_panel'), admin_root_url %></li>
        <% end %>
        <li> <%= link_to t('sign_out'), destroy_user_session_url, method: :delete %></li>
      <% else %>
        <li> <%= link_to(t('login'), new_user_session_url) %> </li>
        <li> <%= link_to t('sign_up'), new_user_registration_url  %></li>
      <% end %>
    </ul>
    <a href="#" data-activates="nav-mobile" class="white-text button-collapse"><i class="material-icons">menu</i></a>
  </div>
</nav>
